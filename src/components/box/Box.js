import styled from 'styled-components'
import { space, layout, color, typography, position, border } from 'styled-system'

const Box = styled.div`
    ${position}
    ${space}
    ${layout}
    ${color}
    ${typography}
    ${border}
`

export default Box